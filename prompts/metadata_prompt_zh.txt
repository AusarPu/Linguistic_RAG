你是一位搜索引擎优化（SEO）与知识库索引构建专家。
我将提供一段来自知识库的文本块及其标识符。
你的任务是生成高质量的元数据，以最大化该文本块在 RAG 系统中的“可检索性”。

请严格遵循以下步骤：

1. **评估索引价值（有意义性判断）**：
   - 判断该文本块是否包含具体的、可检索的事实、定义或逻辑。
   - 如果内容纯粹是导航性的（页眉/页脚）、不连贯的片段、或者缺乏信息密度的通用废话，请标记为 `false`。

2. **提取搜索关键词（基于分类学）**：
   - 如果有意义，请严格按照以下优先级提取 5-10 个关键词/短语：
     - **第一梯队（高优先级）：** 命名实体（具体的人名、机构名、产品名、地名）。
     - **第二梯队（中优先级）：** 领域专业术语、缩写、算法名称或行业黑话。
     - **第三梯队（上下文约束）：** 时间范围（年份）、版本号、错误代码、具体指标数值。
   - **负向约束：** 严禁提取通用的动词（如“分析”、“提高”）或抽象名词（如“方法”、“系统”），除非它们是专有名词的一部分。
   - **目标：** 这些关键词必须能支持针对特定查询的精确词法匹配（BM25）。

3. **生成模拟用户查询（逆向搜索模拟）**：
   - 如果有意义，请生成 3-5 个不同的问题或查询，模拟用户为找到*这一具体*文本块会输入的内容。
   - **风格多样性要求：**
     - **风格 A（标准问句）：** 语法完整的自然语言问题。
     - **风格 B（搜索片段）：** 关键词堆砌的、碎片化的搜索输入。
   - **独立性原则（Self-Containment）：** 严禁使用“本文”、“作者”、“这一段”等代词。你必须将其解析为文本块标识符或内容中明确的具体主语/标题（例如：将“他的理论是什么？”改为“[具体人名]的理论是什么？”）。
   - **特异性原则（Specificity）：** 生成的问题必须是主要由该文本块回答的。如果文本块只讨论了一个细分话题，不要生成过于宽泛的问题。

请以严格的 JSON 格式返回结果，包含以下字段：
- "is_meaningful": Boolean (true/false，表示是否有意义)。
- "reason_if_not_meaningful": String (如果为 false，请简述理由；如果为 true，则为空字符串)。
- "keyword_summaries": List of strings (提取的关键词列表)。如果无意义则为空列表。
- "generated_questions": List of strings (生成的查询列表)。如果无意义则为空列表。

**重要：仅返回纯 JSON 文本。不要使用 Markdown 格式（不要 ```json）。不要添加任何解释性文字。**
确保所有字符串内的特殊字符都已正确转义。

文本块标识符: {chunk_id}
提供的文本块如下：

提供的文本块如下：
```
{text_chunk_content}
```
