You are an expert in Search Engine Optimization (SEO) and Knowledge Graph Indexing.
I will provide you with a text chunk and its identifier from a knowledge base.
Your task is to generate high-quality metadata that maximizes the "retrievability" of this chunk in a RAG system.

Please strictly follow these steps:

1. **Evaluate Indexing Value (Meaningfulness)**:
   - Determine if this chunk contains specific, retrievable facts, definitions, or logic.
   - Mark as `false` if it is purely navigational (headers/footers), disjointed fragments, or generic boilerplate with no factual density.

2. **Extract Search Keywords (Taxonomy-Based)**:
   - If meaningful, extract 5-10 keywords/phrases strictly following this priority hierarchy:
     - **Tier 1 (High Priority):** Named Entities (Specific People, Organizations, Products, Locations).
     - **Tier 2 (Medium Priority):** Domain Specific Terminology, Acronyms, Algorithms, or Professional Jargon.
     - **Tier 3 (Contextual Constraints):** Timeframes (Years), Version Numbers, Error Codes, specific Metrics.
   - **Negative Constraint:** Do NOT extract generic verbs (e.g., "analyze", "improve") or abstract nouns (e.g., "method", "system") unless they are part of a proper noun.
   - **Goal:** These keywords must enable exact lexical matching for specific queries.

3. **Generate Synthetic User Queries (Reverse-HyDE)**:
   - If meaningful, generate 3-5 distinct questions/queries that a user would type to find *this specific* text chunk.
   - **Style Diversity Requirement:**
     - **Style A (Standard Question):** A complete, grammatical question.
     - **Style B (Search Fragment):** A keyword-heavy, broken query simulating a search engine input.
   - **Self-Containment Rule:** NEVER use pronouns like "this text", "the author", or "the article". You MUST resolve them to the specific subject/title provided in the chunk identifier or content (e.g., change "What is his theory?" to "What is [Name]'s theory?").
   - **Specificity Rule:** The generated questions must be *primarily* answerable by this chunk. Do not generate broad questions if the chunk only discusses a specific sub-topic.

Please return the result in strict JSON format with the following fields:
- "is_meaningful": Boolean (true/false).
- "reason_if_not_meaningful": String (brief reason if false, empty string if true).
- "keyword_summaries": List of strings (extracted keywords). Empty if not meaningful.
- "generated_questions": List of strings (synthetic queries). Empty if not meaningful.

**Important: Return RAW JSON only. No Markdown formatting (no ```json). No explanations.**
Ensure all strings are properly escaped.

Text chunk identifier: {chunk_id}

The provided text chunk is as follows:
```
{text_chunk_content}
```
